name: regression test

on:
    # push:
    workflow_dispatch:
            
jobs:
    checkout:
        runs-on: self-hosted
        steps:
            - name: checkout source code
              uses: actions/checkout@v2
    build:
        needs: checkout
        runs-on: self-hosted
        steps:
            - name: run build
              run: |
                make clean
                make
    test:
        needs: build
        runs-on: self-hosted
        steps:
            - name: run test
              run: |
                ./test 9 0
                ./test 9 1
                ./test 9 2
#                ./test 4
#                ./test 5
#                ./test 6
